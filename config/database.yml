default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS", 5) %>
  host: 127.0.0.1
  port: 5432
  username: student
  password: ""

development:
  <<: *default
  database: pos_mockup_backend_development

test:
  <<: *default
  database: pos_mockup_backend_test

production:
  url: <%= ENV["DATABASE_URL"] %>